# Golang Restful Architecture

## Manage dependencies with "dep" tool
1. Install ``dep`` here: https://github.com/golang/dep
2. Run the command: ``dep ensure --update``
3. Compile and run: ``go build && go install -v ./... && ./go-rest-arch``